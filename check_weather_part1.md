### Solution
```JavaScript 
let weather = 'sunny';  // 
if (weather === 'sunny') {
  console.log("It's a beautiful day!");  // It will log
} else if (weather === 'rainy') {
  console.log("Grab your umbrella.");  // If weather assaigned to the rainy then log that
} else {
  console.log("Let's stay inside.");  // If weather assaigned to any other value then log it
}
```
